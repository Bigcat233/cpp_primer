#include <iostream>

using namespace std;

int main()
{
    //double Double; //c++区分大小写，并且很简单的办法是打上去后Double并没有变色
    //the difference between NULL and nullptr：NULL其实就是0，而nullptr表示空指针，在新标准下，最好使用nullptr 避免NULL的使用
    /*const int const_nm = 1;
    const_nm=2;*/ //出错因为const型变量只读
    cout << "Hello world!" << endl;
    return 0;
}
